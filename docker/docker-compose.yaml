services:
  aether-harness:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: aether-harness:latest
    container_name: aether-harness
    working_dir: /workspace
    env_file:
      - ../.env
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /workspace
    volumes:
      - ../:/workspace
    command: ["tail", "-f", "/dev/null"]
    healthcheck:
      test: ["CMD", "python", "-c", "import aether; print('ok')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  aether-tests:
    image: aether-harness:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    profiles: ["tests"]
    working_dir: /workspace
    env_file:
      - ../.env
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /workspace
    volumes:
      - ../:/workspace
    command: >
      sh -lc "
      python -m unittest
      aether.tests.models.test_provider_streaming
      aether.tests.engine.test_engine
      aether.tests.subagents.test_subagents
      aether.tests.runtime.test_state_machine
      "
