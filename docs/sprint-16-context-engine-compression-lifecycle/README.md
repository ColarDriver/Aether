# Sprint 16 - Context Engine and Compression Lifecycle

This folder defines the PR boundary for moving context compression from an
engine-internal mechanism into a runtime subsystem with a pluggable context
engine contract.

Start with `00_overview.md`, then implement PR16.1 through PR16.5 in order.

