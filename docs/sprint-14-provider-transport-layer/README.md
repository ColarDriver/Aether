# Sprint 14 - Provider Transport Layer

This folder defines the PR boundary for extracting provider payload conversion,
tool conversion, response normalization, and provider-specific validation into a
transport layer.

Start with `00_overview.md`, then implement PR14.1 through PR14.5 in order.

